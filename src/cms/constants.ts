/**
 * The frontmatter key the tabs widget nests every idea field under. Must match
 * the `ideaDetails` field in static/admin/config.yml and the IdeaPost entry of
 * NESTED_DETAILS_FIELD in gatsby/constants.js.
 */
export const IDEA_DETAILS_FIELD = "ideaDetails";
